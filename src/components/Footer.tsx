import React from "react";

export default class Footer extends React.Component {
    
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <footer className="fixed-bottom bg-dark justify-content-center">
                <div className="container">
                </div>
            </footer>
        );
    }
}